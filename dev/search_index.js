var documenterSearchIndex = {"docs":
[{"category":"section","location":"self_hosting/#Running-your-own-Nominatim-server-(for-bulk-geocoding)","page":"Running your own server","text":"The public server at nominatim.openstreetmap.org allows one request per second, and jobs that run longer than a day are cut to four requests per minute (usage policy). That is fine for a few thousand addresses and useless for a million.\n\nYour own server has no limits. This guide sets one up for the United States on a Windows 11 PC using WSL2 (Linux inside Windows), without Docker.\n\nTested: Path A was followed end to end in September 2026 on a laptop with an Intel Core Ultra 9 185H, 64 GB RAM and a 1 TB NVMe SSD (WSL 2.7.14, Ubuntu 24.04, PostgreSQL 16, osm2pgsql 1.11, Nominatim 5.3.2). All times and sizes marked measured come from that run. Path B (Docker) and the sections marked untested were not tried.\n\nThe steps work for other regions: swap the download link for another Geofabrik extract.","title":"Running your own Nominatim server (for bulk geocoding)"},{"category":"section","location":"self_hosting/#How-to-read-the-commands","page":"Running your own server","text":"Each gray box is one step. Copy the whole box, paste it into the terminal, press Enter, and wait until the prompt comes back (the line ending in $) before doing the next box.\nThe heading above each box says where it runs: PowerShell (Windows) or Ubuntu (the Linux terminal).\nWhere you should check something, the text says what the output should look like.\nBoxes that start tmux or a long-running program are always on their own: anything pasted after them would be swallowed.\n\n","title":"How to read the commands"},{"category":"section","location":"self_hosting/#1.-What-you-need","page":"Running your own server","text":" Used in the tested run\nWindows Windows 11\nRAM 64 GB (48 GB given to WSL)\nCPU 22 logical cores (10 given to WSL)\nDisk NVMe SSD, ≥ 250 GB free (see below)\nTime ~30 min of setup, then ~15.5 hours of import that runs on its own\n\nA spinning hard disk would take days or weeks. 16 GB of RAM can work, but expect a much slower import.","title":"1. What you need"},{"category":"section","location":"self_hosting/#Disk-space-(measured,-US-extract)","page":"Running your own server","text":"Item Size\nUS OpenStreetMap extract (us-latest.osm.pbf) 12 GB\nFlatnode file (node locations, deleted automatically after loading) 113 GB\nDatabase while loading 65 GB\nPeak extra space used on C: ~200 GB\nFinal database 90 GB\nSpace still taken on C: after the import ~230 GB (see note)\n\nThe WSL virtual disk does not shrink. Linux reported only 106 GB in use after the import, but the virtual disk file on C: stays at its largest size. Plan for that space to stay taken.","title":"Disk space (measured, US extract)"},{"category":"section","location":"self_hosting/#Why-a-12-GB-download-becomes-a-90-GB-database","page":"Running your own server","text":"The .osm.pbf file is heavily compressed. Nominatim unpacks it into PostgreSQL and adds real geometries for every road, building and boundary, search indexes, and a precomputed address chain for every place (house → street → city → county → state).","title":"Why a 12 GB download becomes a 90 GB database"},{"category":"section","location":"self_hosting/#Where-the-data-must-*not*-go","page":"Running your own server","text":"Not in OneDrive (or Dropbox, Google Drive, …).\nNot on /mnt/c/... from inside Ubuntu. Windows folders seen from Linux are very slow. Everything in this guide lives in your Linux home folder (~).","title":"Where the data must not go"},{"category":"section","location":"self_hosting/#Decisions-made-in-this-guide","page":"Running your own server","text":"Whole US extract. It includes Alaska, Hawaii and territories; they are small.\nNo US TIGER data. TIGER adds address ranges along streets, so its results are interpolated estimates (like the Census geocoder). Without it, every house-level result is a real address point from OpenStreetMap. Where OpenStreetMap has no house numbers you get a street-level match instead.\nextratags import style: keeps almost every OpenStreetMap tag (website, phone, opening hours, …).\nNo updates (--no-updates): a frozen snapshot. Smaller, and results stay reproducible. To get newer data, re-import.\n\n","title":"Decisions made in this guide"},{"category":"section","location":"self_hosting/#2.-Before-you-start:-stop-Windows-from-interrupting","page":"Running your own server","text":"The import runs for about 15.5 hours. In the tested run it was interrupted twice: once by an automatic Windows Update restart at 1 AM, once by a forced power-off.\n\nPause Windows Update: Settings → Windows Update → Pause updates → Pause for 5 weeks. Check the date it shows; an old pause may have expired.\nNever sleep when plugged in: Settings → System → Power & battery → Screen, sleep & hibernate timeouts → When plugged in, put my device to sleep after: Never.\nPlug in the laptop, and don't carry it around while the import runs (it works hard and gets hot; hibernating may not work).\nIf you must stop, stop cleanly (see If the import stops (in Path A)).\n\n","title":"2. Before you start: stop Windows from interrupting"},{"category":"section","location":"self_hosting/#Path-A:-WSL2-without-Docker","page":"Running your own server","text":"","title":"Path A: WSL2 without Docker"},{"category":"section","location":"self_hosting/#A1.-Tell-WSL-how-much-memory-and-CPU-it-may-use","page":"Running your own server","text":"Create the file C:\\Users\\<you>\\.wslconfig in Notepad with:\n\n[wsl2]\n# Leave ~12-16 GB for Windows. On a 64 GB machine: 48GB. On 32 GB: 24GB.\nmemory=48GB\n# Leave some cores for Windows.\nprocessors=10\nswap=16GB\n\nDo not add sparseVhd=true: WSL 2.7 disables sparse virtual disks because of a data-corruption risk. During installation you may see \"Sparse VHD support is currently disabled\"; ignore it and don't force it.","title":"A1. Tell WSL how much memory and CPU it may use"},{"category":"section","location":"self_hosting/#A2.-Install-WSL-and-Ubuntu-24.04","page":"Running your own server","text":"PowerShell (run as administrator):\n\nwsl --install -d Ubuntu-24.04\n\nRestart Windows only if it asks (on the tested machine it did not). Ubuntu opens and asks for a Linux user name and password; they don't have to match Windows. You end up at a prompt like yourname@PC:/mnt/c/Users/yourname$.\n\nPowerShell, check it runs as WSL 2 (the VERSION column must say 2):\n\nwsl --list --verbose","title":"A2. Install WSL and Ubuntu 24.04"},{"category":"section","location":"self_hosting/#A3.-Go-to-your-Linux-home-folder","page":"Running your own server","text":"Ubuntu starts in the Windows folder (/mnt/c/...), which is slow. Ubuntu:\n\ncd ~\n\nThe prompt now ends in :~$. Opening Ubuntu later starts in ~ too, unless you open it from a Windows folder.","title":"A3. Go to your Linux home folder"},{"category":"section","location":"self_hosting/#A4.-Check-that-systemd-is-on","page":"Running your own server","text":"Ubuntu:\n\ncat /etc/wsl.conf\n\nYou should see systemd=true under [boot] (Ubuntu 24.04 sets this by default). If it is missing, run this, then wsl --shutdown in PowerShell and reopen Ubuntu:\n\nprintf '[boot]\\nsystemd=true\\n' | sudo tee -a /etc/wsl.conf","title":"A4. Check that systemd is on"},{"category":"section","location":"self_hosting/#A5.-Install-PostgreSQL,-PostGIS,-osm2pgsql-and-tools","page":"Running your own server","text":"Ubuntu (asks for your Linux password; takes a few minutes):\n\nsudo apt-get update && sudo apt-get install -y osm2pgsql postgresql-postgis postgresql-postgis-scripts pkg-config libicu-dev python3-venv python3-dev build-essential wget tmux\n\nUbuntu, check the versions:\n\npsql --version && osm2pgsql --version\n\nExpected: PostgreSQL 16.x and osm2pgsql 1.11.x (Nominatim needs ≥ 12 and ≥ 1.8). The paths below use 16; adjust if yours differs.","title":"A5. Install PostgreSQL, PostGIS, osm2pgsql and tools"},{"category":"section","location":"self_hosting/#A6.-Tune-PostgreSQL-for-the-import","page":"Running your own server","text":"Ubuntu (paste the whole box; it writes one settings file):\n\nsudo tee /etc/postgresql/16/main/conf.d/nominatim.conf > /dev/null << 'EOF'\n# Nominatim tuning (https://nominatim.org/release-docs/latest/admin/Installation/)\n# For WSL with ~48 GB. With 24 GB: maintenance_work_mem = 4GB, effective_cache_size = 12GB.\nshared_buffers = 2GB\nmaintenance_work_mem = 6GB\nautovacuum_work_mem = 2GB\nwork_mem = 50MB\neffective_cache_size = 24GB\nsynchronous_commit = off\nmax_wal_size = 1GB\ncheckpoint_timeout = 60min\ncheckpoint_completion_target = 0.9\nrandom_page_cost = 1.0\nwal_level = minimal\nmax_wal_senders = 0\nEOF\n\nUbuntu, restart PostgreSQL:\n\nsudo systemctl restart postgresql","title":"A6. Tune PostgreSQL for the import"},{"category":"section","location":"self_hosting/#A7.-Create-the-database-users","page":"Running your own server","text":"Type these exactly as shown. $USER is filled in automatically with your Linux user name (check with echo $USER); postgres and www-data are fixed system names, not placeholders.\n\nUbuntu (a database user with full rights, for running the import):\n\nsudo -u postgres createuser -s $USER\n\nUbuntu (a read-only database user the web server uses):\n\nsudo -u postgres createuser www-data\n\nrole \"…\" already exists means that step was already done; carry on.","title":"A7. Create the database users"},{"category":"section","location":"self_hosting/#A8.-Install-Nominatim","page":"Running your own server","text":"Ubuntu (creates a Python environment and installs Nominatim; a few minutes):\n\npython3 -m venv ~/nominatim-venv && ~/nominatim-venv/bin/pip install --upgrade pip && ~/nominatim-venv/bin/pip install \"psycopg[binary]\" nominatim-db nominatim-api falcon uvicorn gunicorn\n\nUbuntu (make the nominatim command available in every new terminal):\n\necho 'source ~/nominatim-venv/bin/activate' >> ~/.bashrc && source ~/nominatim-venv/bin/activate\n\nUbuntu, check:\n\nnominatim --version\n\nExpected: Nominatim version 5.3.x. The prompt now starts with (nominatim-venv).","title":"A8. Install Nominatim"},{"category":"section","location":"self_hosting/#A9.-Download-the-data","page":"Running your own server","text":"Ubuntu, create the project folder:\n\nmkdir -p ~/nominatim-project && cd ~/nominatim-project\n\nUbuntu, download the US extract (12 GB; measured ~10 minutes, but Geofabrik's speed varies). Wait until it shows 100% and the prompt returns:\n\nwget https://download.geofabrik.de/north-america/us-latest.osm.pbf\n\nUbuntu, verify the download:\n\nwget -q https://download.geofabrik.de/north-america/us-latest.osm.pbf.md5 && md5sum -c us-latest.osm.pbf.md5\n\nExpected: us-latest.osm.pbf: OK. If it says FAILED, delete the file (rm us-latest.osm.pbf) and download again.\n\nUbuntu, optional but recommended extra data (better ranking and US ZIP code centroids; about a minute):\n\nwget https://nominatim.org/data/wikimedia-importance.csv.gz && wget -O secondary_importance.sql.gz https://nominatim.org/data/wikimedia-secondary-importance.sql.gz && wget https://nominatim.org/data/us_postcodes.csv.gz\n\nUbuntu, check:\n\nls -lh ~/nominatim-project\n\nExpected: us-latest.osm.pbf (12G), us-latest.osm.pbf.md5, wikimedia-importance.csv.gz (~307M), secondary_importance.sql.gz (~7M), us_postcodes.csv.gz (~390K). Odd dates are normal (they are the files' dates on the server). Files with these names in the project folder are picked up by the import automatically.","title":"A9. Download the data"},{"category":"section","location":"self_hosting/#A10.-Configure-the-import","page":"Running your own server","text":"Ubuntu (one line; writes the settings file .env):\n\nprintf 'NOMINATIM_IMPORT_STYLE=extratags\\nNOMINATIM_FLATNODE_FILE=/home/%s/nominatim-flatnode/flatnode.file\\n' \"$USER\" > ~/nominatim-project/.env && mkdir -p ~/nominatim-flatnode && cat ~/nominatim-project/.env\n\nExpected output (with your user name):\n\nNOMINATIM_IMPORT_STYLE=extratags\nNOMINATIM_FLATNODE_FILE=/home/yourname/nominatim-flatnode/flatnode.file","title":"A10. Configure the import"},{"category":"section","location":"self_hosting/#A11.-Start-the-import","page":"Running your own server","text":"Check section 2 once more (updates paused, sleep off, plugged in).\n\nUbuntu, open a tmux session so the import survives closing the window. Paste only this line:\n\ntmux new -s import\n\nThe window clears and a green bar appears at the bottom: you are inside tmux.\n\nUbuntu (inside tmux), start the import:\n\ncd ~/nominatim-project && nominatim import --osm-file us-latest.osm.pbf --no-updates --threads $(nproc) 2>&1 | tee setup.log\n\nWithin seconds you should see Creating database, Setting up country tables, Importing OSM data file, then a fast-changing Processing: Node(…) counter.\n\nLeave tmux (import keeps running): press Ctrl+B, release, press D.\nCome back later: open Ubuntu and run tmux attach -t import.\nWatch progress without attaching: tail -3 ~/nominatim-project/setup.log.","title":"A11. Start the import"},{"category":"section","location":"self_hosting/#What-happens,-and-how-long-it-takes-(measured)","page":"Running your own server","text":"Phase Log shows Time\nLoad nodes Processing: Node(…) 19 min\nLoad ways (roads, buildings) Way(…) 7 h 27 min\nLoad relations (boundaries, routes) Relation(…) 1 h 16 min\nosm2pgsql clustering and indexing, then the flatnode file is deleted Clustering table…, Building index on table 'planet_osm_ways' ≥ 20 min (not measured to the end: the tested run was interrupted here)\nWikipedia data, tables and functions Importing wikipedia…, Create functions…, Create tables 1 min\nCopy into placex Load data into placex table 16 min\nPostcodes Calculate postcodes 7 min\nIndexing boundaries and ranks 1–25 (states, counties, cities, …) Starting boundaries rank …, Starting rank 5 … rank 25 7 min\nIndexing streets (ranks 26–27) … per second - rank 26 ETA (seconds): … 1 h 31 min\nIndexing houses and POIs (ranks 28–30) Starting rank 30 3 h 25 min\nInterpolation lines, unranked places (rank 0), postcodes Starting interpolation lines, Starting rank 0, Starting postcodes 27 min\nSearch indexes, drop update tables, word counts Post-process tables, Recompute word counts 17 min\nDone Import completed successfully ~15.5 h in total\n\nThe indexing lines show an ETA (seconds) for the current rank only, not the whole import. Resuming an interrupted indexing phase is cheap: in the tested run, already-finished ranks 1–25 were re-checked in under a minute.","title":"What happens, and how long it takes (measured)"},{"category":"section","location":"self_hosting/#If-the-import-stops","page":"Running your own server","text":"Stop cleanly if you have to: tmux attach -t import, then Ctrl+C. After a restart or power loss PostgreSQL recovers by itself (in the tested run it did, twice). Then find the last phase in the log:\n\nUbuntu:\n\ngrep -a -E \"^20[0-9-]+ [0-9:]+: [A-Z]\" ~/nominatim-project/setup.log | tail -5\n\nThen pick the matching case. Run the command inside a new tmux new -s import session, as in A11.\n\nStopped during Indexing places / Starting rank … or later: resume indexing (already indexed places are kept):\n\ncd ~/nominatim-project && nominatim import --continue indexing --no-updates --threads $(nproc) 2>&1 | tee -a setup.log\n\nStopped during Load data into placex table or Calculate postcodes:\n\ncd ~/nominatim-project && nominatim import --continue load-data --no-updates --threads $(nproc) 2>&1 | tee -a setup.log\n\nStopped during Processing: Node/Way/Relation: start over.\n\ndropdb nominatim; rm -f ~/nominatim-flatnode/flatnode.file\n\nthen repeat A11.\n\nStopped after the loading finished but before Load data into placex table (the last lines are Building index on table 'planet_osm_ways', Importing wikipedia importance data, or Create functions/Create tables): this happened in the tested run. --continue load-data alone fails here, because it skips creating Nominatim's tables. Instead of re-importing (~9 hours), run the missing setup steps first; see Advanced: resuming after the data load near the end of this guide.","title":"If the import stops"},{"category":"section","location":"self_hosting/#A12.-Check-the-database","page":"Running your own server","text":"When the log ends with Import completed successfully, Ubuntu:\n\ncd ~/nominatim-project && nominatim admin --check-database\n\nExpected: every line ends in OK, except wikipedia/wikidata data … not applicable and TIGER external data table … not applicable (both normal here).\n\nThe flatnode file was already deleted by the import (--no-updates). The download is no longer needed; free 12 GB inside Linux with:\n\nrm ~/nominatim-project/us-latest.osm.pbf\n\n(This does not shrink the virtual disk file on C:; see Disk space in section 1.)","title":"A12. Check the database"},{"category":"section","location":"self_hosting/#A13.-Create-a-start-script-for-the-server","page":"Running your own server","text":"Ubuntu (paste the whole box; it writes the file ~/start-nominatim.sh):\n\ncat > ~/start-nominatim.sh << 'EOF'\n#!/bin/bash\n# Start the local Nominatim server on http://127.0.0.1:8088\nWORKERS=\"${NOMINATIM_WORKERS:-8}\"\nsudo -n systemctl start postgresql 2>/dev/null   # usually already running\nsource ~/nominatim-venv/bin/activate\ncd ~/nominatim-project\necho \"Starting Nominatim on http://127.0.0.1:8088 with $WORKERS workers (log: ~/nominatim-project/server.log)\"\nexec gunicorn -b 127.0.0.1:8088 -w \"$WORKERS\" -k uvicorn.workers.UvicornWorker \\\n     \"nominatim_api.server.falcon.server:run_wsgi()\" >> ~/nominatim-project/server.log 2>&1\nEOF\n\n8 workers suit 10 CPU cores for WSL; with more cores, set e.g. NOMINATIM_WORKERS=12 before starting.","title":"A13. Create a start script for the server"},{"category":"section","location":"self_hosting/#A14.-Start,-check-and-stop-the-server","page":"Running your own server","text":"Ubuntu, start it in the background (you can close the window afterwards):\n\ntmux new-session -d -s nominatim-server bash ~/start-nominatim.sh\n\nCheck it: after about 10 seconds open http://127.0.0.1:8088/status in a Windows browser. Expected: OK. Try http://127.0.0.1:8088/search?q=350+5th+Ave+New+York&format=jsonv2.\n\nUse 127.0.0.1 rather than localhost: with some Windows tools (e.g. curl.exe) localhost added ~220 ms per request.\n\nUbuntu, stop it:\n\ntmux kill-session -t nominatim-server\n\nPowerShell, free WSL's memory completely (also stops PostgreSQL and the server):\n\nwsl --shutdown\n\nAfter a Windows restart or wsl --shutdown, start the server again with the first command of this step. The database stays on disk.\n\n","title":"A14. Start, check and stop the server"},{"category":"section","location":"self_hosting/#Using-the-server-from-Julia","page":"Running your own server","text":"using Nominatim\n\nclient = NominatimClient(base_url = \"http://127.0.0.1:8088\")\nserver_status(client)\n\ngeocode(\"1600 Pennsylvania Ave NW, Washington, DC 20500\"; client)\n\nresults = geocode_batch(addresses_dataframe; client,\n                        countrycodes = \"us\",\n                        concurrent_requests = 12,\n                        checkpoint_file = \"addresses.checkpoint.jsonl\")\n\nMeasured throughput and tips are on the bulk geocoding page: about 13 queries/s right after the server starts, ~50/s after the first ~1,500 new addresses, ~100/s for addresses it has seen before.\n\n","title":"Using the server from Julia"},{"category":"section","location":"self_hosting/#Advanced:-resuming-after-the-data-load","page":"Running your own server","text":"Use this only for the case described in If the import stops (in Path A): osm2pgsql finished loading (the log shows Processed … relations), but the import stopped before Load data into placex table. It runs the steps that nominatim import performs between osm2pgsql and loading, taken from Nominatim 5.3.2's SetupAll._base_import, then continues normally. Tested once, on 5.3.2.\n\nUbuntu, check that the loaded data is there and Nominatim's tables are not (expect a number in the millions, then f):\n\npsql -d nominatim -Atc \"SELECT reltuples::bigint FROM pg_class WHERE relname = 'place'\" && psql -d nominatim -Atc \"SELECT to_regclass('public.placex') IS NOT NULL\"\n\nUbuntu, the flatnode file is not needed anymore; delete it (~113 GB):\n\nrm -f ~/nominatim-flatnode/flatnode.file\n\nUbuntu (paste the whole box; writes a small Python script):\n\ncat > ~/nominatim-project/finish_base_import.py << 'EOF'\n\"\"\"Run the part of `nominatim import` between osm2pgsql and `--continue load-data`.\"\"\"\nimport logging, sys\nfrom pathlib import Path\nfrom nominatim_db.clicmd.setup import SetupAll\nfrom nominatim_db.config import Configuration\nfrom nominatim_db.data import country_info\nfrom nominatim_db.db.connection import connect\nfrom nominatim_db.tools import refresh\n\nlogging.basicConfig(stream=sys.stderr, format='%(asctime)s: %(message)s',\n                    datefmt='%Y-%m-%d %H:%M:%S', level=logging.WARNING)\nproject_dir = Path.cwd().resolve()\nconfig = Configuration(project_dir)\ncountry_info.setup_country_config(config)\ndsn = config.get_libpq_dsn()\n\nwith connect(dsn) as conn, conn.cursor() as cur:\n    cur.execute(\"SELECT to_regclass('public.placex') IS NOT NULL\")\n    if cur.fetchone()[0]:\n        sys.exit('placex already exists: run nominatim import --continue load-data instead.')\n\nlogging.warning('Importing wikipedia importance data')\nrefresh.import_wikipedia_articles(dsn, Path(config.WIKIPEDIA_DATA_PATH or project_dir))\nlogging.warning('Importing secondary importance raster data')\nrefresh.import_secondary_importance(dsn, project_dir)\nSetupAll()._setup_tables(config, False)\nlogging.warning('Done. Now run: nominatim import --continue load-data --no-updates')\nEOF\n\nUbuntu, run it (a few minutes; ends with Done. Now run: …):\n\ncd ~/nominatim-project && ~/nominatim-venv/bin/python3 finish_base_import.py 2>&1 | tee -a setup.log\n\nThen, in a tmux new -s import session, continue:\n\ncd ~/nominatim-project && nominatim import --continue load-data --no-updates --threads $(nproc) 2>&1 | tee -a setup.log\n\n","title":"Advanced: resuming after the data load"},{"category":"section","location":"self_hosting/#Path-B:-Docker-(untested)","page":"Running your own server","text":"Untested: this path was not tried. It follows the mediagis/nominatim-docker how-to for image version 5.3. Docker Desktop needs a paid subscription for organisations with more than 250 employees or more than USD 10 million revenue.\n\nThe mediagis/nominatim image downloads the data, imports it and starts the API in one command. It runs on the same WSL2 machinery, so the disk, memory and \"stop Windows from interrupting\" advice above applies.\n\nCreate .wslconfig as in step A1.\nInstall Docker Desktop with the default \"Use WSL 2 based engine\".\n\nPowerShell (one command; the backticks continue the line):\n\ndocker run -d --name nominatim `\n  --shm-size=16g `\n  -e PBF_URL=https://download.geofabrik.de/north-america/us-latest.osm.pbf `\n  -e IMPORT_STYLE=extratags `\n  -e IMPORT_WIKIPEDIA=true `\n  -e IMPORT_SECONDARY_WIKIPEDIA=true `\n  -e IMPORT_US_POSTCODES=true `\n  -e IMPORT_TIGER_ADDRESSES=false `\n  -e FREEZE=true `\n  -e THREADS=10 `\n  -e GUNICORN_WORKERS=8 `\n  -e POSTGRES_MAINTENANCE_WORK_MEM=6GB `\n  -e POSTGRES_EFFECTIVE_CACHE_SIZE=24GB `\n  -e NOMINATIM_PASSWORD=choose_a_password `\n  -v nominatim-data:/var/lib/postgresql/16/main `\n  -v nominatim-flatnode:/nominatim/flatnode `\n  -p 8080:8080 `\n  mediagis/nominatim:5.3\n\nPowerShell, watch the import (Ctrl+C stops watching, not the import):\n\ndocker logs -f nominatim\n\nIt is done when http://127.0.0.1:8080/status answers OK. Stop and start later with docker stop nominatim and docker start nominatim; the data stays in the nominatim-data volume.\n\n","title":"Path B: Docker (untested)"},{"category":"section","location":"self_hosting/#Maintenance-and-troubleshooting","page":"Running your own server","text":"","title":"Maintenance and troubleshooting"},{"category":"section","location":"self_hosting/#Common-problems","page":"Running your own server","text":"Symptom Cause and fix\nLaptop restarted overnight Windows Update. Pause updates (section 2), then see If the import stops (in Path A).\n.env missing, or commands after tmux new did nothing Several commands were pasted together with tmux new. Paste one box at a time.\nnominatim: command not found Run source ~/nominatim-venv/bin/activate.\nImport stops with \"No space left on device\" Disk full. Free space on C:, then see If the import stops (in Path A).\nImport killed, dmesg mentions \"Out of memory\" Lower maintenance_work_mem or --threads, or raise memory= in .wslconfig.\nImport very slow (days) Data is on a hard disk or under /mnt/c. Keep it in the Linux home folder on an SSD.\nLog says checkpoints are occurring too frequently Harmless during the import.\nhttp://127.0.0.1:8088 not reachable from Windows The server is not running (after a restart or wsl --shutdown): start it (A14).\nAPI returns Database connection failed PostgreSQL is not running: sudo systemctl start postgresql.\npermission denied for table … in the API The www-data database user is missing: sudo -u postgres createuser www-data, then nominatim refresh --website.","title":"Common problems"},{"category":"section","location":"self_hosting/#Refreshing-the-data","page":"Running your own server","text":"With --no-updates the database cannot follow OpenStreetMap changes. For newer data, download a fresh extract and import again into a new database. Record the data date with your results: server_status(client).data_updated in Julia.","title":"Refreshing the data"},{"category":"section","location":"self_hosting/#Adding-TIGER-later","page":"Running your own server","text":"TIGER needs the update tables, so it only works on a database imported without --no-updates. See the Nominatim TIGER docs. Nominatim.jl flags interpolated results either way.","title":"Adding TIGER later"},{"category":"section","location":"self_hosting/#Moving-WSL-to-another-drive-(untested)","page":"Running your own server","text":"PowerShell (needs WSL 2.5 or newer; check with wsl --version):\n\nwsl --shutdown\n\nwsl --manage Ubuntu-24.04 --move E:\\WSL\\Ubuntu-24.04","title":"Moving WSL to another drive (untested)"},{"category":"section","location":"self_hosting/#Getting-disk-space-back-on-C:","page":"Running your own server","text":"The virtual disk file keeps its largest size. In the tested run it was 226 GB after the import while Linux used only 94 GB. Compacting it shrank the file to 95 GB and freed 131 GB on C: (measured), in about 15 minutes. The database passed nominatim admin --check-database afterwards.\n\nStop the server first (A14). Optional but sensible: make a backup copy (about the size of the used space), and delete it once everything works again:\n\nwsl --export Ubuntu-24.04 \"$env:USERPROFILE\\ubuntu-nominatim-backup.tar\"\n\nUbuntu, delete the download if it is still there:\n\nrm -f ~/nominatim-project/us-latest.osm.pbf\n\nUbuntu, tell the virtual disk which space is empty (asks for your password):\n\nsudo fstrim -av\n\nExpected: a line like /: 901.7 GiB (…) trimmed on /dev/sdd. The large number is the empty part of the virtual disk's 1 TB maximum; nothing is deleted.\n\nPowerShell, close the Ubuntu window, then:\n\nwsl --shutdown\n\nPowerShell, find the virtual disk file (copy the path it prints):\n\n(Get-ChildItem -Path HKCU:\\Software\\Microsoft\\Windows\\CurrentVersion\\Lxss | Where-Object { $_.GetValue(\"DistributionName\") -eq 'Ubuntu-24.04' }).GetValue(\"BasePath\") + \"\\ext4.vhdx\"\n\nPowerShell as administrator (right-click PowerShell → Run as administrator):\n\ndiskpart\n\nAt the DISKPART> prompt, type these one at a time, with your path in the first line:\n\nselect vdisk file=\"C:\\Users\\yourname\\AppData\\Local\\wsl\\{…}\\ext4.vhdx\"\n\nattach vdisk readonly\n\ncompact vdisk\n\nWait for DiskPart successfully compacted the virtual disk file. Do not open Ubuntu meanwhile.\n\ndetach vdisk\n\nexit","title":"Getting disk space back on C:"},{"category":"section","location":"self_hosting/#Not-using-your-own-PC","page":"Running your own server","text":"Everything in Path A (from A4 on) works on any Ubuntu 24.04 machine, such as a university server or a cloud VM with enough SSD space. Point base_url at that machine, behind a firewall or SSH tunnel: never expose an open geocoder to the internet.\n\n","title":"Not using your own PC"},{"category":"section","location":"self_hosting/#Sources","page":"Running your own server","text":"Nominatim installation and Ubuntu 24.04 guide\nNominatim import\nmediagis/nominatim-docker how-to\nWSL configuration and WSL disk space\nGeofabrik US extract\nNominatim usage policy","title":"Sources"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Server-and-settings","page":"API reference","text":"","title":"Server and settings"},{"category":"section","location":"api/#One-query-at-a-time","page":"API reference","text":"","title":"One query at a time"},{"category":"section","location":"api/#Many-queries-at-once","page":"API reference","text":"","title":"Many queries at once"},{"category":"section","location":"api/#Results","page":"API reference","text":"","title":"Results"},{"category":"section","location":"api/#Match-quality","page":"API reference","text":"","title":"Match quality"},{"category":"module","location":"api/#Nominatim","page":"API reference","text":"Nominatim\n\nA readable Julia client for Nominatim, the open-source geocoder built on OpenStreetMap data.\n\nIt works against the public server (https://nominatim.openstreetmap.org, rate-limited to one request per second) and, more usefully for large jobs, against your own Nominatim server, where there is no limit.\n\nEvery request asks Nominatim for everything it can return: the address broken into parts, extra OSM tags (website, opening hours, …), all name variants, entrances, and the full geometry.\n\nSearch by free text or by address fields → search, geocode\nCoordinates to address → reverse_geocode\nKnown OSM objects → lookup, place_details\nMany addresses at once, resumable → geocode_batch, reverse_geocode_batch\nResults as a table → places_dataframe\nServer health → server_status\n\n\n\n\n\n","title":"Nominatim"},{"category":"type","location":"api/#Nominatim.NominatimClient","page":"API reference","text":"NominatimClient(; base_url, user_agent, email, accept_language,\n                  minimum_seconds_between_requests, readtimeout_seconds,\n                  maximum_retries, cache)\n\nSettings shared by every request to one Nominatim server.\n\nKeywords\n\nbase_url - the server, e.g. \"http://127.0.0.1:8088\" for your own server. Default: the environment variable NOMINATIM_URL, otherwise the public server https://nominatim.openstreetmap.org.\nuser_agent - identifies your application, e.g. \"MyStudy/1.0 (me@university.edu)\". Required for the public server (its usage policy blocks generic User-Agents). Default: ENV[\"NOMINATIM_USER_AGENT\"], or \"Nominatim.jl\" for your own server.\nemail - optional contact address sent with each request, which the public server's operators use to reach you instead of blocking you. Default: ENV[\"NOMINATIM_EMAIL\"].\naccept_language - preferred language of names and addresses (\"en\").\nminimum_seconds_between_requests - 1.0 for the public server (and it may not be lower), 0.0 for any other server.\nreadtimeout_seconds - give up if the server sends nothing for this long (60).\nmaximum_retries - retries after network errors, HTTP 429 and HTTP 5xx (3).\ncache - where to store responses so repeated queries don't hit the server:\n:auto (default): on for the public server (the usage policy requires caching), off for your own server;\ntrue: on, in this package's scratch space;\nfalse: off;\na folder path: on, in that folder.\n\nExamples\n\npublic_client = NominatimClient(user_agent = \"HousingStudy/1.0 (me@university.edu)\")\nlocal_client  = NominatimClient(base_url = \"http://127.0.0.1:8088\")\n\n\n\n\n\n","title":"Nominatim.NominatimClient"},{"category":"function","location":"api/#Nominatim.default_client","page":"API reference","text":"default_client() -> NominatimClient\n\nThe client used when you don't pass client = …. It is created on first use from the environment variables NOMINATIM_URL, NOMINATIM_USER_AGENT and NOMINATIM_EMAIL, or set explicitly with set_default_client!.\n\n\n\n\n\n","title":"Nominatim.default_client"},{"category":"function","location":"api/#Nominatim.set_default_client!","page":"API reference","text":"set_default_client!(client::NominatimClient) -> NominatimClient\n\nUse client for every call that does not pass client = … explicitly.\n\nset_default_client!(NominatimClient(base_url = \"http://127.0.0.1:8088\"))\ngeocode(\"350 5th Ave, New York, NY\")   # goes to your own server\n\n\n\n\n\n","title":"Nominatim.set_default_client!"},{"category":"function","location":"api/#Nominatim.server_status","page":"API reference","text":"server_status(client = default_client()) -> NamedTuple\n\nWhether the server is working, and how fresh its data is: (ok, status, message, data_updated, software_version, database_version).\n\ndata_updated is the date of the OpenStreetMap data (a DateTime, UTC); record it with your results so they are reproducible.\n\n\n\n\n\n","title":"Nominatim.server_status"},{"category":"type","location":"api/#Nominatim.NominatimError","page":"API reference","text":"NominatimError(http_status, message, url)\n\nThrown when a Nominatim server answers with an error instead of results, for example a malformed request (HTTP 400), a block for exceeding the usage policy (HTTP 403 or 429), or a server problem that persisted through all retries.\n\nFields\n\nhttp_status::Int - the HTTP status code the server returned.\nmessage::String  - the server's explanation.\nurl::String      - the request that failed.\n\n\n\n\n\n","title":"Nominatim.NominatimError"},{"category":"function","location":"api/#Nominatim.clear_cache!","page":"API reference","text":"clear_cache!(client::NominatimClient = default_client()) -> Int\n\nDelete every cached response of client and return how many were deleted.\n\n\n\n\n\n","title":"Nominatim.clear_cache!"},{"category":"function","location":"api/#Nominatim.search","page":"API reference","text":"search(query::AbstractString; client, limit, countrycodes, viewbox, bounded,\n       layer, featuretype, exclude_place_ids, dedupe, polygon_threshold,\n       extra_parameters) -> Vector{Place}\n\nsearch(; street, city, county, state, country, postalcode, amenity,\n       client, limit, …) -> Vector{Place}\n\nFind places matching a free-text query, or a structured address. Returns all candidates, best first (empty if nothing matched). Every result carries the full detail Nominatim can return (see Place).\n\nStructured search is usually more precise for addresses: Nominatim does not have to guess which part of the text is the city.\n\nKeywords\n\nclient = default_client() - the server to ask (see NominatimClient).\nlimit = 10 - maximum number of results (1–40).\ncountrycodes - restrict to countries, e.g. \"us\" or [\"us\", \"ca\"].\nviewbox = (min_longitude, min_latitude, max_longitude, max_latitude) - prefer results in this box; with bounded = true, only return results inside it.\nlayer - \"address\", \"poi\", \"railway\", \"natural\", \"manmade\", or a vector.\nfeaturetype - \"country\", \"state\", \"city\" or \"settlement\".\nexclude_place_ids - place ids to skip (for paging through results).\ndedupe = true - merge duplicates (e.g. one street split into several ways).\npolygon_threshold - simplify returned geometries to this tolerance (degrees).\nextra_parameters = Dict() - any other Nominatim parameter, passed through.\nStructured form only: street (house number and street name), city, county, state, country, postalcode, amenity (name or type of POI). Structured search cannot be combined with a free-text query.\n\nExamples\n\nsearch(\"Empire State Building\")\nsearch(street = \"350 5th Ave\", city = \"New York\", state = \"NY\", country = \"us\")\nsearch(\"coffee\"; viewbox = (-74.02, 40.70, -73.93, 40.80), bounded = true, limit = 40)\n\n\n\n\n\n","title":"Nominatim.search"},{"category":"function","location":"api/#Nominatim.geocode","page":"API reference","text":"geocode(query::AbstractString; kwargs...) -> Union{Nothing, Place}\ngeocode(; street, city, state, postalcode, country, …) -> Union{Nothing, Place}\n\nThe best match for an address, or nothing if there is none. Takes the same keywords as search.\n\nCheck place.match_level before trusting the coordinates: a street-level match (:street) means the house number was not found.\n\nplace = geocode(\"350 5th Ave, New York, NY 10118\")\nplace.name, place.latitude, place.longitude, place.match_level\n\n\n\n\n\n","title":"Nominatim.geocode"},{"category":"function","location":"api/#Nominatim.reverse_geocode","page":"API reference","text":"reverse_geocode(latitude, longitude; client, zoom = 18, layer, polygon_threshold,\n                extra_parameters) -> Union{Nothing, Place}\n\nThe address (or object) at a location, or nothing if there is nothing there (e.g. the open ocean).\n\nzoom sets the level of detail: 18 = building, 17 = street, 16 = major street, 14 = neighbourhood, 13 = village/suburb, 10 = city, 8 = county, 5 = state, 3 = country.\n\nlayer limits what may be returned: \"address\", \"poi\", \"railway\", \"natural\", \"manmade\" (default: address and POI).\n\nreverse_geocode(40.74844, -73.98566)          # Empire State Building\nreverse_geocode(40.74844, -73.98566; zoom = 10)  # New York\n\n\n\n\n\n","title":"Nominatim.reverse_geocode"},{"category":"function","location":"api/#Nominatim.lookup","page":"API reference","text":"lookup(osm_ids; client, polygon_threshold, extra_parameters) -> Vector{Place}\n\nFetch known OpenStreetMap objects with full detail. osm_ids is a vector of \"N123\" / \"W123\" / \"R123\" codes, (type, id) pairs, or Places. Longer lists are split into requests of 50 automatically. Objects the server does not know are silently left out.\n\nlookup([\"W34633854\", \"R19761182\"])   # Empire State Building, White House\n\n\n\n\n\n","title":"Nominatim.lookup"},{"category":"function","location":"api/#Nominatim.place_details","page":"API reference","text":"place_details(osm_id; client, category) -> Dict{String, Any}\nplace_details(place::Place; client) -> Dict{String, Any}\n\nEverything Nominatim stores about one object, from its /details endpoint: all names and address tags, the computed postcode, the full address hierarchy (\"address\"), linked places, keywords, dependent addresses (\"hierarchy\"), entrances and geometry.\n\nThe output format differs from Place and is returned as plain Dicts. Meant for inspecting individual results, not for bulk use.\n\nosm_id is \"W34633854\" or (\"way\", 34633854). category picks the entry when one OSM object has several main tags (e.g. \"tourism\").\n\n\n\n\n\n","title":"Nominatim.place_details"},{"category":"function","location":"api/#Nominatim.geocode_batch","page":"API reference","text":"geocode_batch(addresses; client, concurrent_requests, checkpoint_file,\n              countrycodes, viewbox, bounded, layer, featuretype,\n              polygon_threshold, extra_parameters, expand,\n              progress_every_seconds) -> DataFrame\n\nGeocode many addresses and return one row per input, in input order.\n\nInput\n\na vector of address strings, or\na DataFrame / vector of NamedTuples with a free-text column (query, address or q), or structured columns street, city, county, state, country, postalcode (zip works too), amenity.\n\nStructured columns usually match more precisely.\n\nOutput columns\n\ninput_index, input_query - which input the row belongs to.\nstatus - \"matched\", \"no_match\", \"error\" or \"empty_input\".\nerror_message - why a query failed (missing otherwise).\nrequested_house_number, house_number_matches - the house number found in the input, and whether the result has it (see house_number_matches).\nthen every column of places_dataframe: name, coordinates, match level, likely_interpolated, address parts, bounding box, extra tags, …\n\nKeywords\n\nclient = default_client() - use your own server for large jobs.\nconcurrent_requests = 1 - simultaneous requests. On your own server, up to about the number of API worker processes. Always 1 on the public server.\ncheckpoint_file = nothing - a file path (e.g. \"run.checkpoint.jsonl\"). Every answer is appended to it as it arrives; run the same call again after an interruption and answered queries are not sent again. Queries that failed are retried on the next run.\ncountrycodes, viewbox, bounded, layer, featuretype, polygon_threshold, extra_parameters - as in search, applied to every query. For US addresses, countrycodes = \"us\" avoids matches abroad.\nexpand = false - add one column per tag, as in places_dataframe.\nprogress_every_seconds = 30 - how often to log progress.\n\nExample\n\nusing Nominatim, DataFrames\n\nclient = NominatimClient(base_url = \"http://127.0.0.1:8088\")\naddresses = DataFrame(street = [\"350 5th Ave\", \"1600 Pennsylvania Ave NW\"],\n                      city = [\"New York\", \"Washington\"], state = [\"NY\", \"DC\"],\n                      zip = [10118, 20500])\n\nresults = geocode_batch(addresses; client, countrycodes = \"us\",\n                        concurrent_requests = 12,\n                        checkpoint_file = \"addresses.checkpoint.jsonl\")\n\nexact = filter(row -> row.match_level == \"house_or_poi\" && row.house_number_matches === true &&\n                      !row.likely_interpolated, results)\n\n\n\n\n\n","title":"Nominatim.geocode_batch"},{"category":"function","location":"api/#Nominatim.reverse_geocode_batch","page":"API reference","text":"reverse_geocode_batch(latitudes, longitudes; client, zoom = 18, layer,\n                      concurrent_requests, checkpoint_file, polygon_threshold,\n                      extra_parameters, expand, progress_every_seconds) -> DataFrame\n\nFind the address at many locations. Returns one row per coordinate pair, in input order, with input_index, input_latitude, input_longitude, status (\"matched\", \"no_match\", \"error\", \"empty_input\" for missing/NaN coordinates), error_message, then every column of places_dataframe.\n\nKeywords as in reverse_geocode and geocode_batch.\n\n\n\n\n\n","title":"Nominatim.reverse_geocode_batch"},{"category":"type","location":"api/#Nominatim.Place","page":"API reference","text":"Place\n\nOne result from Nominatim.\n\nWhat is there\n\nname::String - name of the object, e.g. \"Empire State Building\" (\"\" if unnamed).\ndisplay_name::String - the full, human-readable address.\ncategory::String, place_type::String - the main OpenStreetMap tag, e.g. \"office\" / \"government\", \"building\" / \"house\", \"highway\" / \"residential\".\naddresstype::String - what the object counts as in an address (\"road\", \"city\", …).\n\nWhere it is\n\nlatitude::Float64, longitude::Float64 - the point Nominatim returns (the centroid for areas and lines).\nboundingbox::Union{Missing, BoundingBox}\ngeometry_geojson::Union{Missing, String} - the full geometry (point, line or polygon) as GeoJSON text.\n\nAddress and extra information\n\naddress::Dict{String, String} - the address split into parts: house_number, road, neighbourhood, suburb, city/town/village, county, state, ISO3166-2-lvl4, postcode, country, country_code, …\nextratags::Dict{String, String} - further OpenStreetMap tags: website, phone, opening_hours, wikidata, building:levels, …\nnamedetails::Dict{String, String} - every name variant: name:en, short_name, official_name, ref, …\nentrances::Vector{Dict{String, Any}} - tagged entrances of a building.\n\nMatch quality\n\nplace_rank::Int - Nominatim's address rank, 30 = house/POI, 26–27 = street.\nmatch_level::Symbol - place_rank in words: :house_or_poi, :street, :locality, :neighbourhood, :suburb, :city, :county, :state, :country or :other.\nlikely_interpolated::Bool - true when the house number is probably estimated from an address range (OSM interpolation line or US TIGER data) rather than a mapped address point. See is_likely_interpolated.\nimportance::Union{Missing, Float64} - Nominatim's relevance score (0–1).\n\nIdentifiers\n\nosm_type::String (\"node\", \"way\", \"relation\"), osm_id::Int - the OpenStreetMap object; view it at https://www.openstreetmap.org/<osm_type>/<osm_id>.\nplace_id::Int - Nominatim's internal id. Not stable between servers or imports.\nlicence::String - the data licence notice.\nraw - the original JSON object, for anything not covered above.\n\n\n\n\n\n","title":"Nominatim.Place"},{"category":"type","location":"api/#Nominatim.BoundingBox","page":"API reference","text":"BoundingBox(south, north, west, east)\n\nThe rectangle around a place, in degrees (WGS84). For a house it is tiny; for a street, city or state it covers the whole area.\n\n\n\n\n\n","title":"Nominatim.BoundingBox"},{"category":"function","location":"api/#Nominatim.places_dataframe","page":"API reference","text":"places_dataframe(places; expand = false) -> DataFrame\n\nA table with one row per result. nothing entries (no match) become rows of missing, so the output lines up with the input.\n\nColumns:\n\nwhat is there: name, display_name, category, place_type, addresstype\nwhere: latitude, longitude, bbox_south, bbox_north, bbox_west, bbox_east, geometry_geojson\nmatch quality: match_level, place_rank, likely_interpolated, importance\naddress: address_house_number, address_road, address_neighbourhood, address_suburb, address_hamlet, address_village, address_town, address_city, address_county, address_state, address_state_code (e.g. \"US-NY\"), address_postcode, address_country, address_country_code\nidentifiers: osm_type, osm_id, place_id\neverything else, as JSON text: address_json (all address parts), extratags_json, namedetails_json, entrances_json\n\nWith expand = true, every address part, extra tag and name variant that occurs in any result also gets its own column, prefixed address_, tag_ and name_ (e.g. tag_website, tag_opening_hours, name_short_name).\n\n\n\n\n\n","title":"Nominatim.places_dataframe"},{"category":"function","location":"api/#Nominatim.match_level_for_rank","page":"API reference","text":"match_level_for_rank(place_rank::Integer) -> Symbol\n\nTranslate Nominatim's address rank into a readable match level, following https://nominatim.org/release-docs/latest/customize/Ranking/:\n\nrank level\n28–30 :house_or_poi\n26–27 :street\n25 :locality\n22–24 :neighbourhood\n17–21 :suburb\n13–16 :city\n10–12 :county\n5–9 :state\n4 :country\nother :other\n\nA search for \"123 Main St, Springfield, IL\" that only finds the street returns rank 26, i.e. :street: the coordinates are somewhere on Main Street, not at number 123.\n\n\n\n\n\n","title":"Nominatim.match_level_for_rank"},{"category":"function","location":"api/#Nominatim.is_likely_interpolated","page":"API reference","text":"is_likely_interpolated(category, place_type, osm_type) -> Bool\n\ntrue when the result is probably an estimated position on an address range rather than a mapped address point.\n\nNominatim returns such results as category = \"place\", type = \"house\", with osm_type = \"way\": the way is either an OpenStreetMap interpolation line or, for US TIGER data, the street (https://nominatim.org/release-docs/latest/api/Output/).\n\nThis is a heuristic: Nominatim has no field that states the source directly, and a rare untagged area carrying only an address would also match. Mapped address points come back as nodes (place=house on a node) or as buildings (category = \"building\").\n\n\n\n\n\n","title":"Nominatim.is_likely_interpolated"},{"category":"function","location":"api/#Nominatim.house_number_matches","page":"API reference","text":"house_number_matches(place::Place, requested_house_number) -> Union{Missing, Bool}\n\nWhether the result carries the house number you asked for.\n\nmissing if you did not ask for a house number.\nfalse if the result has no house number (e.g. a street-level match) or a different one.\ntrue if it is the same number, one of several listed (\"10;12\"), or inside a listed range (\"10-20\").\n\n\n\n\n\n","title":"Nominatim.house_number_matches"},{"category":"function","location":"api/#Nominatim.extract_house_number","page":"API reference","text":"extract_house_number(address_text) -> String\n\nThe house number at the start of a street address, or \"\" if there is none.\n\nextract_house_number(\"350 5th Ave\")          # \"350\"\nextract_house_number(\"1600A Pennsylvania\")   # \"1600A\"\nextract_house_number(\"12-14 Main St\")        # \"12-14\"\nextract_house_number(\"Main St\")              # \"\"\n\n\n\n\n\n","title":"Nominatim.extract_house_number"},{"category":"section","location":"bulk/#Bulk-geocoding","page":"Bulk geocoding","text":"Nominatim has no batch endpoint, so geocode_batch sends one request per address. How fast that goes depends entirely on the server:\n\nServer Speed Suitable for\nPublic (nominatim.openstreetmap.org) 1 query/second, one at a time; jobs longer than a day are cut to 4/minute up to a few thousand addresses\nYour own as fast as your machine answers, many at a time millions\n\nTo set up your own server, see Running your own server.","title":"Bulk geocoding"},{"category":"section","location":"bulk/#Input","page":"Bulk geocoding","text":"A vector of strings:\n\nresults = geocode_batch([\"350 5th Ave, New York, NY 10118\",\n                         \"1600 Pennsylvania Ave NW, Washington, DC 20500\"])\n\nOr a DataFrame (or vector of NamedTuples) with structured columns, which usually match more precisely:\n\nusing DataFrames\n\naddresses = DataFrame(\n    street = [\"350 5th Ave\", \"1600 Pennsylvania Ave NW\"],\n    city   = [\"New York\", \"Washington\"],\n    state  = [\"NY\", \"DC\"],\n    zip    = [10118, 20500],          # integer ZIP codes keep their leading zeros\n)\nresults = geocode_batch(addresses; countrycodes = \"us\")\n\nRecognised columns (case-insensitive):\n\nfree text: query, address or q\nstructured: street (house number and street), city, county, state, country, postalcode (or zip, zipcode, postcode, postal_code), amenity\n\nUse one kind or the other; other columns are ignored.","title":"Input"},{"category":"section","location":"bulk/#Output","page":"Bulk geocoding","text":"One row per input, in input order:\n\nColumn \ninput_index, input_query which input the row belongs to\nstatus \"matched\", \"no_match\", \"error\", \"empty_input\"\nerror_message why a query failed\nrequested_house_number, house_number_matches the house number in your input, and whether the result has it\nname, display_name, category, place_type, addresstype what is there\nlatitude, longitude \nmatch_level, place_rank, likely_interpolated, importance match quality\naddress_house_number … address_country_code address parts\nbbox_south, bbox_north, bbox_west, bbox_east bounding box\nosm_type, osm_id, place_id identifiers\naddress_json, extratags_json, namedetails_json, entrances_json, geometry_geojson everything else, as JSON text\n\nWith expand = true, every extra tag and name variant gets its own column too (tag_website, tag_phone, name_short_name, …).\n\nKeep only exact address-point matches:\n\nexact = filter(results) do row\n    row.status == \"matched\" &&\n    row.match_level == \"house_or_poi\" &&\n    row.house_number_matches === true &&\n    row.likely_interpolated === false\nend","title":"Output"},{"category":"section","location":"bulk/#Large-jobs-on-your-own-server","page":"Bulk geocoding","text":"using Nominatim, DataFrames, CSV\n\nclient = NominatimClient(base_url = \"http://127.0.0.1:8088\")\naddresses = CSV.read(\"addresses.csv\", DataFrame)\n\nresults = geocode_batch(addresses; client,\n                        countrycodes = \"us\",\n                        concurrent_requests = 12,\n                        checkpoint_file = \"addresses.checkpoint.jsonl\")\n\nCSV.write(\"addresses_geocoded.csv\", results)\n\nconcurrent_requests: how many queries run at once. Start around the number of API worker processes (-w in the gunicorn command, or GUNICORN_WORKERS for Docker) and watch the queries/second in the progress log; more is not faster once the server is saturated. The public server always gets 1.\ncheckpoint_file: every answer is appended to this file the moment it arrives. If the run stops (crash, Ctrl+C, restart), run the same call again: answered queries are read back from the file and only the rest is sent. Queries that ended in an error are not stored, so they are retried. An entry is only reused if the query at that position is unchanged.\nProgress is logged every 30 seconds (progress_every_seconds).\nRecord server_status(client).data_updated with your results: it is the date of the OpenStreetMap data you geocoded against.\n\nThe checkpoint file stores the raw server answers (a few kB per address), so it doubles as a complete, re-parseable record of the run.","title":"Large jobs on your own server"},{"category":"section","location":"bulk/#How-fast-is-it?","page":"Bulk geocoding","text":"Measured on a laptop (Intel Core Ultra 9 185H, NVMe SSD, WSL2 with 48 GB RAM and 10 cores, 8 gunicorn workers, US database of 90 GB), structured US addresses, concurrent_requests = 12:\n\nSituation Throughput 1 million addresses\nServer just started (database not in memory) 12–15 queries/s at first —\nAfter ~1,500 new addresses ~50 queries/s ~5.5 h\nAddresses the server has seen before ~100 queries/s ~2.8 h\nAverage over the first 2,000 new addresses 25–31 queries/s —\n\nThe first minutes are slow while PostgreSQL reads the database into memory; throughput then keeps rising. Plan a large run as one long job rather than many short ones with server restarts in between.\nMore than about 12 concurrent requests did not help with 8 server workers.\nRequesting full detail (extra tags, name variants, geometry) costs no measurable time: the database search dominates.\nSorting the input by state / ZIP code / street does not help. In repeated cold-start runs on the same 2,040 addresses (60 ZIP codes), shuffled order was as fast or slightly faster (25–27 vs 31 queries/s).\nNominatim has no multi-address request, and one would not help: sending the request and parsing the JSON take under a millisecond, the search itself 20–150 ms.","title":"How fast is it?"},{"category":"section","location":"bulk/#Coordinates-to-addresses","page":"Bulk geocoding","text":"results = reverse_geocode_batch(latitudes, longitudes; client,\n                                concurrent_requests = 12,\n                                checkpoint_file = \"points.checkpoint.jsonl\")\n\nMissing or NaN coordinates get status = \"empty_input\".","title":"Coordinates to addresses"},{"category":"section","location":"#Nominatim.jl","page":"Home","text":"A readable Julia client for Nominatim, the open-source geocoder built on OpenStreetMap data. Free, no API key.","title":"Nominatim.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/eohne/Nominatim.jl\")","title":"Installation"},{"category":"section","location":"#Choosing-a-server","page":"Home","text":"using Nominatim\n\n# The public server: identify your application (required by its usage policy).\n# Limited to 1 request per second; responses are cached on disk.\nclient = NominatimClient(user_agent = \"HousingStudy/1.0 (me@university.edu)\")\n\n# Your own server (see \"Running your own server\"): no limits, no cache.\nclient = NominatimClient(base_url = \"http://127.0.0.1:8088\")\n\n# Use it everywhere without passing client = … each time:\nset_default_client!(client)\n\nThe environment variables NOMINATIM_URL, NOMINATIM_USER_AGENT and NOMINATIM_EMAIL set the defaults too.","title":"Choosing a server"},{"category":"section","location":"#One-address","page":"Home","text":"place = geocode(street = \"350 5th Ave\", city = \"New York\", state = \"NY\",\n                postalcode = \"10118\", country = \"us\")\n\nPlace: Empire State Building\n  display_name: Empire State Building, 350, 5th Avenue, Koreatown, …, New York, 10118, United States\n  coordinates:  40.7484421, -73.9856589\n  what:         office=yes\n  match_level:  :house_or_poi\n  boundingbox:  BoundingBox(south=40.7479255, north=40.7489585, west=-73.9865012, east=-73.9848166)\n  osm:          way 34633854\n\nEverything Nominatim returns is in the Place:\n\nplace.name                      # \"Empire State Building\"\nplace.latitude, place.longitude\nplace.boundingbox\nplace.address[\"house_number\"]   # \"350\"\nplace.address[\"postcode\"]       # \"10118\"\nplace.extratags[\"website\"]      # \"https://www.esbnyc.com/explore\"\nplace.extratags[\"opening_hours\"]\nplace.namedetails[\"short_name\"] # \"ESB\"\nplace.geometry_geojson          # the building outline as GeoJSON\nplace.raw                       # the original JSON\n\nFree text works too, but structured fields are usually more precise:\n\ngeocode(\"350 5th Ave, New York, NY 10118\")\nsearch(\"Main St, Springfield\"; countrycodes = \"us\", limit = 10)   # all candidates","title":"One address"},{"category":"section","location":"#Coordinates-to-address","page":"Home","text":"reverse_geocode(40.74844, -73.98566)             # building level\nreverse_geocode(40.74844, -73.98566; zoom = 10)  # city level","title":"Coordinates to address"},{"category":"section","location":"#Is-the-match-exact?","page":"Home","text":"Nominatim always returns something close if it can. A request for a house number it doesn't know usually returns the street. Three fields tell you what you got:\n\nField Meaning\nmatch_level :house_or_poi (a house number, building or POI), :street, :locality, :neighbourhood, :suburb, :city, :county, :state, :country\nlikely_interpolated true if the position is estimated from an address range (OpenStreetMap interpolation line or US TIGER data) instead of a mapped address point\nhouse_number_matches (batch results) whether the result has the house number you asked for\n\nplace = geocode(street = \"123 Main St\", city = \"Springfield\", state = \"IL\", country = \"us\")\nplace.match_level                                  # :street → not at number 123\nNominatim.house_number_matches(place, \"123\")       # false\n\nFor exact, address-point results keep rows where match_level == :house_or_poi, house_number_matches == true and likely_interpolated == false.\n\nlikely_interpolated is a heuristic: Nominatim has no field that states the source directly. It follows the documented output of interpolated results (place=house on a way).","title":"Is the match exact?"},{"category":"section","location":"#Many-addresses","page":"Home","text":"See Bulk geocoding.","title":"Many addresses"},{"category":"section","location":"#Other-endpoints","page":"Home","text":"lookup([\"W34633854\", \"R19761182\"])       # known OpenStreetMap objects, 50 per request\nplace_details(place)                      # everything stored about one object\nserver_status(client)                     # is it up, and how fresh is the data?\nplaces_dataframe([place1, place2])        # results as a table","title":"Other endpoints"},{"category":"section","location":"#Data-licence","page":"Home","text":"Data © OpenStreetMap contributors, available under the Open Database License. Publications using the results must credit OpenStreetMap.","title":"Data licence"}]
}
